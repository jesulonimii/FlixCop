const express = require('express');
const router = express.Router()


router.post('/detect/image', (req, res) => {
    //res.json(req.actors)
    //TODO: use google search by image to find actors
})





module.exports = router;